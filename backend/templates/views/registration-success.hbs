<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Successful</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #2e5ab9;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            text-align: center;
            background: white;
            padding: 40px;
            border: 5px solid #4CAF50;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            width: 80%; /* Set width for responsiveness */
            max-width: 600px; /* Max width for larger screens */
        }
        h1 {
            color: #4CAF50;
        }
        p {
            font-size: 1.2em;
        }
        .exploration {
            margin-top: 20px;
            display: flex;
            flex-wrap: wrap; /* Allow wrapping of boxes */
            justify-content: space-between; /* Space between boxes */
        }
        .exploration h2 {
            margin-bottom: 20px;
            font-size: 1.5em;
            width: 100%; /* Ensure h2 spans the full width */
        }
        .feature-box {
            background-color: #f0f0f0;
            border: 2px solid #4CAF50;
            border-radius: 8px;
            padding: 15px;
            margin: 10px;
            flex: 1 1 45%; /* Flex-grow, flex-shrink, and base width */
            text-align: center;
            transition: transform 0.3s, box-shadow 0.3s; /* Smooth transitions */
        }
        .feature-box:hover {
            transform: translateY(-5px); /* Slight lift on hover */
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2); /* Shadow on hover */
        }
        .feature-box a {
            color: #007BFF;
            text-decoration: none;
            font-weight: bold;
        }
        button {
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
        }
        button a {
            color: white; /* Ensure link is white on the button */
            text-decoration: none;
        }
        button:hover {
            background-color: #45a049; /* Darken button on hover */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🎉 Registration Successful!</h1>
        <p>Thank you for joining us. We're excited to have you on board!</p>
        <div class="exploration">
            <h2>Explore Our University</h2>
            <div class="feature-box"><a href="/faculties">Faculty Members</a></div>
            <div class="feature-box"><a href="/sport-clubs">Join the Sport Clubs</a></div>
            <div class="feature-box"><a href="/cultural-events">Explore Our Cultural Events</a></div>
            <div class="feature-box"><a href="/hostel-facilities">Explore Our Hostel Facilities</a></div>
        </div>
        <button><a href="/">Go to Homepage</a></button>
    </div>
</body>
</html>
